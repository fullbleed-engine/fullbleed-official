# fullbleed_audit_contract

Embedded, deterministic audit/compliance contract payloads and fingerprints
used by the Fullbleed PDF engine.

Most users should depend on the top-level `fullbleed` crate instead of using
this implementation crate directly.

Licensed under the MIT License.
